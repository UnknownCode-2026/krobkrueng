import type {Metadata} from "next";import ToolExplorer from "@/components/ToolExplorer";import {tools} from "@/data/tools";export const metadata:Metadata={title:"เครื่องมือทั้งหมด",description:"ค้นหาและเลือกใช้เครื่องมือออนไลน์ภาษาไทยของครบเครื่อง"};export default async function ToolsPage({searchParams}:{searchParams:Promise<{q?:string;cat?:string;status?:string}>}){const p=await searchParams;return <section className="container page-section"><div className="page-hero"><span className="eyebrow">ALL TOOLS / {tools.length}</span><h1>เครื่องมือทั้งหมด</h1><p>ค้นหาหรือกรองตามหมวดหมู่ เครื่องมือที่พร้อมใช้งานทำงานได้ทันทีโดยไม่ต้องสมัคร</p></div><ToolExplorer initialQuery={p.q||""} initialCategory={p.cat||""} initialStatus={p.status||""}/></section>}
