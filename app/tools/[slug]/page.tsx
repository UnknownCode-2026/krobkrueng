import type {Metadata} from "next";import Link from "next/link";import {notFound} from "next/navigation";import ToolCard from "@/components/ToolCard";import ToolWorkspace from "@/components/ToolWorkspace";import {getTool,tools} from "@/data/tools";
export function generateStaticParams(){return tools.map(t=>({slug:t.slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const{slug}=await params;const tool=getTool(slug);if(!tool)return{};return{title:tool.name,description:tool.description,alternates:{canonical:"/tools/"+slug},openGraph:{title:tool.name+" | ครบเครื่อง",description:tool.description,url:"/tools/"+slug}}}
export default async function ToolPage({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const tool=getTool(slug);if(!tool)notFound();const related=tools.filter(t=>t.categorySlug===tool.categorySlug&&t.slug!==tool.slug).slice(0,3);return <section className="container tool-page"><nav className="breadcrumb"><Link href="/">หน้าแรก</Link><span>›</span><Link href={"/categories/"+tool.categorySlug}>{tool.category}</Link><span>›</span><span>{tool.name}</span></nav><div className="tool-title"><div><span className="eyebrow">{tool.category} ↗</span><h1>{tool.name}</h1><p>{tool.description}</p></div><div className="tool-title-icon">{tool.icon}</div></div><div className="workspace-label"><span>พื้นที่ทำงานใช้ฟรี · ไม่ต้องสมัคร</span><span className={tool.status==="ready"?"status ready":"status"}>{tool.status==="ready"?"● พร้อมใช้":"● BETA"}</span></div><ToolWorkspace tool={tool}/><section className="tool-info"><div><span className="eyebrow">PRIVACY FIRST</span><h2>ข้อมูลของคุณอยู่กับคุณ</h2></div><p>เครื่องมือ V1 ที่รองรับจะคำนวณ สร้าง QR และประมวลผลรูปในเบราว์เซอร์เป็นหลัก เว็บไซต์ไม่บันทึกค่าที่กรอกเพื่อคำนวณ</p></section>{related.length>0&&<section className="related-section"><div className="section-heading"><div><span className="eyebrow">RELATED</span><h2>เครื่องมือที่เกี่ยวข้อง</h2></div></div><div className="tools-grid">{related.map(t=><ToolCard key={t.slug} tool={t}/>)}</div></section>}</section>}
