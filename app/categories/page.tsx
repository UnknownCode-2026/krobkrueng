import type {Metadata} from "next";import Link from "next/link";import {categories,tools} from "@/data/tools";export const metadata:Metadata={title:"หมวดหมู่เครื่องมือ",description:"เลือกเครื่องมือครบเครื่องตามหมวดหมู่"};export default function CategoriesPage(){return <section className="container page-section"><div className="page-hero"><span className="eyebrow">CATEGORIES</span><h1>หมวดหมู่เครื่องมือ</h1><p>รวมเครื่องมือเป็นหมวดตามงานที่ใช้บ่อย เพื่อค้นหาได้เร็วทั้งบนมือถือและคอมพิวเตอร์</p></div><div className="category-grid large">{categories.map(c=>{const n=tools.filter(t=>t.categorySlug===c.slug).length;return <Link className="category-card" href={"/categories/"+c.slug} key={c.slug}><span className="category-icon">{c.icon}</span><div><h3>{c.name}</h3><p>{c.description}</p></div><b>{n}</b><span className="card-arrow">↗</span></Link>})}</div></section>}
