import type {Metadata} from "next";import {notFound} from "next/navigation";import ToolCard from "@/components/ToolCard";import {categories,getCategory,tools} from "@/data/tools";export function generateStaticParams(){return categories.map(c=>({slug:c.slug}))}export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const{slug}=await params;const c=getCategory(slug);return c?{title:c.name,description:c.description}:{}}export default async function CategoryPage({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const c=getCategory(slug);if(!c)notFound();const list=tools.filter(t=>t.categorySlug===slug);return <section className="container page-section"><div className="page-hero category-page-hero"><span className="category-icon big">{c.icon}</span><span className="eyebrow">CATEGORY / {list.length}</span><h1>{c.name}</h1><p>{c.description}</p></div><div className="tools-grid">{list.map(t=><ToolCard key={t.slug} tool={t}/>)}</div></section>}
